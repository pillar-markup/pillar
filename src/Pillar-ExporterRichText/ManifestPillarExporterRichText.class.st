"
!! Pillar rendering in-image
This package allows to render (a subset) of """"Pillar"""". It is done in the class ==PRRichTextComposer==, which at the class side have several examples.


The pillar is rendered into a ==Text== object with ==TextAttribute=='s.


"
Class {
	#name : #ManifestPillarExporterRichText,
	#superclass : #PackageManifest,
	#category : #'Pillar-ExporterRichText-Manifest'
}
